echo "pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Hello from Jenkins pipeline!'
            }
        }
    }
}" > Jenkinsfile

git add Jenkinsfile
git commit -m "Add Jenkinsfile"
git push
